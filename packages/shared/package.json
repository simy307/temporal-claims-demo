{
  "name": "@claims/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared request/response types, stage definitions and constants for the Temporal claims demo",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}
